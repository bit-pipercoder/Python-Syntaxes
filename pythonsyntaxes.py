# all of the syntaxes here are examples

print(“Hello, World!”) # prints hello, world to the output

# next syntaxes

name = “bernard” # creates a variable called name so you can use it in prints

print(””) prints something to the output

print() can also print a variable, number, etc.

print(name) # prints the variable name to the output

# name contains bernard so it prints bernard

# concatenation

# concatenation is using + to connect a string to a variable

# for example:

print(“What is your name? “ + name)

print(“What is your name? “ + name) # prints What is your name? Bernard

example = input(“Enter something: “) # allows you to type something in the output

print(len(name)) # prints the length of the name variable

# bernard has 7 characters so this prints 7

# sets

test = {
“Test”
} # this is a set, not a dictionary

# dictionaries

example = {
“name”: “Bernard”,
“language”: “Python”
} # dictionaries store things using keys and values

you can print something from the dictionary like this:

print(example[“name”]) # prints Bernard

# fun fact: concatenation is useful

# you can use it with a variable containing a long paragraph

Nerds = “Your long paragraph here”

print(“Your print thing here on first “ + Nerds)

# import

import math # imports a module/library

# you can use import to import built-in or external libraries

# range

range(0, 10) # creates a range from 0 to 9

# the 10 isnt included

# thats all i know for python so far

# streaming my python journey on youtube.com

# PROPER ARRANGEMENT

print() # prints a blank line
print(””) # prints something to the output
range(0, 10) # makes a range from 0 to 9
variableName = yourVariableThing # creates a variable containing something
input(””) # allows the person to type something
len(something) # gets the length of something
import math # imports a library/module

# dictionary

example = {
“Example”: “Test”
} # dictionary with a key and value

# sets

example = {
“Example”,
“Test”
} # set containing multiple values

# thats it for now